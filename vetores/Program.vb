Imports System

Module Program
    Sub Main(args As String())
        dim nomecompleto(2) as String
        
        nomecompleto(0) = "Adenilton"
        nomecompleto(1) = "Brito"

        console.writeline(nomecompleto(0) & (1))
        console.writeline(nomecompleto(1))

    End Sub
End Module
